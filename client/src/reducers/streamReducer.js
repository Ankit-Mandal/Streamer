import {
  CREATE_STREAM,
  FETCH_STREAMS,
  FETCH_STREAM,
  EDIT_STREAM,
  DELETE_STREAM,
} from "../actions/types";

export default (state = {}, action) => {
  switch (action.type) {
    case CREATE_STREAM:

    case FETCH_STREAMS:

    case FETCH_STREAM:

    case EDIT_STREAM:

    case DELETE_STREAM:

    default:
      return state;
  }
};
